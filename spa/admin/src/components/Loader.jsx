import React from 'react'

const Loader = () => {
    return (
        <div className='otp-shield_loader'>
            <img src={ OTP_SHIELD.asseturl + '/img/loader.gif'} />
        </div>
     );
}

export default Loader;